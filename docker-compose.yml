version: '3.8'
services:
  rec_sys:
    build:
      context: .
      dockerfile: setup/Dockerfile_api
    volumes:
      - .:/workspace
    ports:
      - "8081:8081"
#    command: "python src/main.py --skip-training"
